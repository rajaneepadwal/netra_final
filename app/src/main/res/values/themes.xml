<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Level1App" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <style name="Theme.Robot.Space" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowBackground">@color/bg_primary</item>

        <item name="colorPrimary">@color/accent_space</item>
        <item name="colorSecondary">@color/accent_space_pressed</item>

        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>

        <item name="materialButtonStyle">@style/NoTintButton</item>
    </style>

    <!-- Button style with NO tint -->
    <style name="NoTintButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@null</item>
    </style>

</resources>
